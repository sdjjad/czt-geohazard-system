# 函数接口

## MapView

```csharp
// 属性
Esri.ArcGISRuntime.UI.Controls.MapView EsriControl { get; }

// 图层
Task<string> AddVectorLayerAsync(string name, string filePath, VectorSymbol? style);
Task<string> AddRasterLayerAsync(string name, string filePath);
Task RemoveLayerAsync(string layerId);
Task ZoomToLayerAsync(string layerId);
Task ClearAllLayersAsync();

// 符号
Task UpdateLayerStyleAsync(MapLayer layer);
Task ApplyFieldSymbologyAsync(string layerId, string fieldName, List<Color> ramp);

// 事件
event Action<double> ScaleChanged;               // 比例尺分母
event Action<double, double> CoordinateChanged;  // 经度, 纬度
```

## MapLayerService

```csharp
// 属性
ObservableCollection<MapLayer> Layers { get; }
event Action LayersChanged;

// 构造
MapLayerService(MapView mapView);

// 图层操作
Task<MapLayer?> AddLayerAsync(string filePath);   // 自动判断矢量/栅格, 自动去重
Task RemoveLayerAsync(MapLayer layer);
Task ClearAllAsync();

// 视图
Task ZoomToLayerAsync(MapLayer layer);
void SetLayerVisibility(MapLayer layer, bool visible);

// 排序
void MoveLayerUp(MapLayer layer);
void MoveLayerDown(MapLayer layer);
void MoveLayerTo(MapLayer layer, int targetIndex);

// 符号
Task ApplyFieldSymbologyAsync(MapLayer layer, string fieldName, List<Color> ramp);
```

## GeoAnalysisService

```csharp
// 核心分析
Task<List<StatResult>> RunAnalysisAsync(
    MapLayer classLayer,            // 分类面图层
    MapLayer hazardLayer,           // 灾害点图层
    string classField,              // 分类字段, ""=自动检测
    AnalysisConfig config,          // 分析配置
    ClassificationMethod classMethod = None,  // 分级方法
    int classCount = 5,             // 分类数
    Action<string>? progress = null // 进度回调
);

// 保存结果 (CSV + JSON + HTML + 4xPNG)
void SaveResults(
    string folder,
    string module,
    List<StatResult> results,
    string? classLayerPath = null,
    string? hazardLayerPath = null
);
```

## Reclassifier (分级分区)

```csharp
enum ClassificationMethod { None, NaturalBreaks, EqualInterval, Quantile, StandardDeviation }

static List<double> ComputeBreaks(List<double> values, ClassificationMethod method, int classCount);
// 输入: 数值列表, 方法, 分类数 → 输出: classCount+1个断点

static string GetClassLabel(double value, List<double> breaks);
// 输入: 数值, 断点 → 输出: "12.50 - 25.00"

static bool IsNumericField(FieldType ft);
```

## AttributeTableGenerator

```csharp
static Task<(List<string> generated, int cleaned)> GenerateAllAsync(
    string spatialRoot,
    string attrRoot,
    Action<int, int, string>? progress = null
);

static Task<string> GenerateCsvAsync(string shpPath, string csvPath);
static string? InferAttrRoot(string spatialRoot);
static (int shp, int tif) CountSpatialFiles(string root);
```

## ChartImageService

```csharp
static void GenerateBarChart(string path, List<StatResult> results, string title);
static void GenerateCfBarChart(string path, List<StatResult> results, string title);
static void GeneratePieChart(string path, List<StatResult> results, string title);
static void GenerateCompositeChart(string path, List<StatResult> results, string title);
```

## ThematicMapService

```csharp
class ThematicMapConfig
{
    string Title, Subtitle, OutputFolder;
    int ImageWidth, ImageHeight;
    bool IncludeLegend, IncludeScaleBar, IncludeNorthArrow, IncludeGrid;
}

ThematicMapService(MapView mapView);
Task<string> ExportThematicMapAsync(ThematicMapConfig config, Action<string>? progress);
Task<string> ExportLegendAsync(ThematicMapConfig config, Action<string>? progress);
```

## MainWindow (主要公开方法)

```csharp
// 面板控制 (XAML x:Name)
DataPanelAnchor.Show() / .Hide()
LayerPanelAnchor.Show() / .Hide()
SymbolPanelAnchor.Show() / .Hide()  // + .IsActive/.IsSelected 前置
GeoPanelAnchor.Show() / .Hide()

// 工具路由 (Button.Click → OpenTool_Click → 读Tag → switch)
// Tag: ImportData, SpatialQuery, SpatialAnalysis, Mapping,
//      AttributeBrowse, AttributeQuery, AttributeManage,
//      SoilTypeAnalysis, SoilMoisture, VegType, VegCoverage, NDVI,
//      StatChart, StatTable, ThematicMap

// 符号编辑器
void ShowSymbolEditor(MapLayer layer);  // 构建单一符号 + 按字段配色UI

// 右键菜单
void ShowLayerContextMenu(MapLayer layer);
ContextMenu CreateDataItemContextMenu(string path, SpatialDataType type);

// 属性表快捷打开
async void OpenAttributeTableForFile(string filePath);
// 自动加载图层到地图(如尚未加载) → 打开属性浏览 → 选中该图层
```

## 工具视图公共方法

```csharp
// AttributeTableView / AttributeManageView
void SetLayerService(MapLayerService);
void RefreshLayerList();
void SelectLayerByFilePath(string path);  // 右键菜单→自动选中

// AttributeQueryView / SpatialQueryView
void SetLayerService(MapLayerService);
void Refresh();

// GeoProcessToolView
void SetLayerService(MapLayerService);
void LoadTool(ModuleInfo module);
void RefreshLayerList();

// ThematicMapToolView
void SetMapView(MapView);
void SetLayerService(MapLayerService);
void LoadTool(string toolName);
```
