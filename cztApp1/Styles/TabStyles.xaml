<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Ribbon tab header -->
    <Style x:Key="RibbonTabItem" TargetType="TabItem">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#555555"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="14,5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="tb" Background="{TemplateBinding Background}"
                            BorderThickness="0" Padding="{TemplateBinding Padding}"
                            Margin="0,1,0,0">
                        <StackPanel>
                            <ContentPresenter x:Name="cp" ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"
                                              TextBlock.FontSize="{TemplateBinding FontSize}"/>
                            <Border x:Name="ul" Height="2" Background="Transparent"
                                    Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ul" Property="Background" Value="#1565C0"/>
                            <Setter TargetName="cp" Property="TextBlock.Foreground" Value="#1565C0"/>
                            <Setter TargetName="cp" Property="TextBlock.FontWeight" Value="Bold"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="cp" Property="TextBlock.Foreground" Value="#1565C0"/>
                            <Setter TargetName="ul" Property="Background" Value="#CCCCCC"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Group label at bottom of button group -->
    <Style x:Key="GrpLbl" TargetType="TextBlock">
        <Setter Property="FontSize" Value="9"/>
        <Setter Property="Foreground" Value="#999999"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,3,0,1"/>
    </Style>

    <!-- TabControl with clean chrome -->
    <Style x:Key="RibbonTC" TargetType="TabControl">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="#F2F2F2">
                            <TabPanel x:Name="HeaderPanel" Panel.ZIndex="1"
                                      IsItemsHost="True" KeyboardNavigation.TabIndex="1"
                                      Margin="2,1,0,0"/>
                        </Border>
                        <Border Grid.Row="1" Background="White" CornerRadius="6"
                                Margin="6,2,6,4" Padding="10,8">
                            <Border.Effect>
                                <DropShadowEffect Color="#18000000" BlurRadius="6"
                                                  ShadowDepth="2" Direction="270"
                                                  Opacity="0.4"/>
                            </Border.Effect>
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
